import TextWrapper from "@shared-components/text-wrapper/TextWrapper";
import * as React from "react";
import { SafeAreaView } from "react-native";

const StripScreen = () => {
  return (
    <SafeAreaView>
      <TextWrapper>Trip</TextWrapper>
    </SafeAreaView>
  );
};
export default StripScreen;
