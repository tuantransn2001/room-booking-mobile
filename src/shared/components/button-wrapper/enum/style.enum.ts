export enum Type {
  primary = "primary",
  success = "success",
  info = "info",
  warning = "warning",
  danger = "danger",
  basic = "basic",
}

export enum Appearance {
  ghost = "ghost",
  outline = "outline",
  filled = "filled",
}

export enum Size {
  tiny = "tiny",
  small = "small",
  medium = "medium",
  large = "large",
}

export enum Status {}
